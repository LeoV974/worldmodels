# MRF Structure Learning via EM + LASSO

Infers graph topology and affinity matrices from Glauber dynamics trajectories using an alternating minimization approach between the graph structure and the affinity matrix. Where the affinity matrix is infered through an Expectation Maximization subroutine.
